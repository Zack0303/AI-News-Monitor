<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ item.get("title", "AI Insight") }} | AI-News-Monitor</title>
  <meta name="description" content="{{ item.get('summary_cn', '') }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=IBM+Plex+Sans:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="../assets/style.css" />
  <script type="application/ld+json">{{ detail_structured_data }}</script>
</head>
<body>
  <main class="page">
    <section class="panel">
      <p><a class="link-btn" href="../index.html">返回首页</a></p>
      <h1>{{ item.get("title", "Untitled") }}</h1>
      <p class="meta">
        <span>Source: {{ item.get("source", "-") }}</span>
        <span>Score: {{ item.get("total_score", 0) }}</span>
        <span>Personalized: {{ item.get("personalized_total_score", item.get("total_score", 0)) }}</span>
      </p>
      <p class="summary">{{ item.get("summary_cn", "") }}</p>
      <h3>为什么重要</h3>
      <p class="reason">{{ item.get("why_it_matters", "") }}</p>
      <h3>下一步建议</h3>
      <p class="action">{{ item.get("next_action", "") }}</p>
      <h3>推荐依据</h3>
      <p class="reason">{{ item.get("preference_explain", "no-preference-signal") }}</p>
      <p class="muted">Generated at: {{ generated_at }}</p>
      <p><a href="{{ item.get('link', '#') }}" target="_blank" rel="noopener noreferrer">查看原文</a></p>
    </section>
  </main>
</body>
</html>

